"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useItem = useItem;

var _context = require("../../ui/context");

var styles = _interopRequireWildcard(require("../styles"));

var _context2 = require("../context");

var _useCx = require("../../utils/hooks/use-cx");

/**
 * External dependencies
 */
// eslint-disable-next-line no-restricted-imports

/**
 * Internal dependencies
 */
function useItem(props) {
  const {
    isAction = false,
    as: asProp,
    className,
    role = 'listitem',
    size: sizeProp,
    ...otherProps
  } = (0, _context.useContextSystem)(props, 'Item');
  const {
    spacedAround,
    size: contextSize
  } = (0, _context2.useItemGroupContext)();
  const size = sizeProp || contextSize;
  const as = asProp || isAction ? 'button' : 'div';
  const cx = (0, _useCx.useCx)();
  const classes = cx(isAction && styles.unstyledButton, styles.itemSizes[size] || styles.itemSizes.medium, styles.item, spacedAround && styles.spacedAround, className);
  const wrapperClassName = cx(styles.itemWrapper);
  return {
    as,
    className: classes,
    wrapperClassName,
    role,
    ...otherProps
  };
}
//# sourceMappingURL=hook.js.map