"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _element = require("@wordpress/element");

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/extends"));

var _compose = require("@wordpress/compose");

var _context = require("../ui/context");

var _segmentedControlButton = _interopRequireDefault(require("./segmented-control-button"));

var _segmentedControlContext = require("./segmented-control-context");

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */
function getShowSeparator(segmentedControlContext, index) {
  var _items$index, _items;

  const {
    currentId,
    items
  } = segmentedControlContext;
  const isLast = index === items.length - 1;
  const isActive = ((_items$index = items[index]) === null || _items$index === void 0 ? void 0 : _items$index.id) === currentId;
  const isNextActive = ((_items = items[index + 1]) === null || _items === void 0 ? void 0 : _items.id) === currentId;
  let showSeparator = true;

  if (items.length < 3) {
    showSeparator = false;
  }

  if (isActive || isNextActive || isLast) {
    showSeparator = false;
  }

  return showSeparator;
}

function SegmentedControlOption(props, forwardedRef) {
  const segmentedControlContext = (0, _segmentedControlContext.useSegmentedControlContext)();
  const id = (0, _compose.useInstanceId)(SegmentedControlOption, segmentedControlContext.baseId || 'segmented-control-option');
  const buttonProps = (0, _context.useContextSystem)({ ...props,
    id
  }, 'SegmentedControlOption');
  const index = segmentedControlContext.items.findIndex(item => item.id === buttonProps.id);
  const showSeparator = getShowSeparator(segmentedControlContext, index);
  return (0, _element.createElement)(_segmentedControlButton.default, (0, _extends2.default)({
    ref: forwardedRef
  }, segmentedControlContext, buttonProps, {
    showSeparator
  }));
}

var _default = (0, _context.contextConnect)(SegmentedControlOption, 'SegmentedControlOption');

exports.default = _default;
//# sourceMappingURL=segmented-control-option.js.map